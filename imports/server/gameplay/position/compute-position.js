export const computePosition = function(target){
    var now = new Date();
    // target.lastPosition;
    // target.lastTimeUpdated;
    // target.currentVelocity;
    // target.finalWantedPosition;    
}